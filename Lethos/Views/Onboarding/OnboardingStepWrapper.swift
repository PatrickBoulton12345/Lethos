import SwiftUI

// Chrome (back button, ticker, dots, continue button) is now owned directly by
// OnboardingContainerView so it stays fixed while only the content slides.
// This file is intentionally empty.
